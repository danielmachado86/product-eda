quarkus.kafka-streams.bootstrap-servers=localhost:29092
quarkus.kafka-streams.application-server=localhost:8080
quarkus.kafka-streams.topics=products,validated_products
quarkus.kafka-streams.schema-registry-url=http://localhost:8081

# pass-through options
kafka-streams.cache.max.bytes.buffering=10240
kafka-streams.commit.interval.ms=1000
kafka-streams.metadata.max.age.ms=500
kafka-streams.auto.offset.reset=earliest
kafka-streams.metrics.recording.level=DEBUG
kafka-streams.default.value.serde=io.confluent.kafka.streams.serdes.protobuf.KafkaProtobufSerde
kafka-streams.default.key.serde=org.apache.kafka.common.serialization.Serdes$StringSerde